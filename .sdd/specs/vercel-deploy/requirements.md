# Requirements Document

## Project Description (Input)
Item 10 — Deploy Vercel: Configure `vercel.json`, set environment variables (`GOOGLE_SHEETS_ID`, `GOOGLE_API_KEY`) in the Vercel dashboard, restrict the API key to the Vercel domain as an HTTP referrer in Google Cloud Console, and verify ISR revalidation works correctly after deployment.

## Requirements
<!-- Will be generated in /sdd:spec-requirements phase -->
